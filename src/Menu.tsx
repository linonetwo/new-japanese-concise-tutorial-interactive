import { FunctionComponent } from 'react';
import styled from 'styled-components';

const ToggleMenu = styled.button``;

const Menu: FunctionComponent<{}> = props => {
  return <ToggleMenu>三</ToggleMenu>;
};

export default Menu;
